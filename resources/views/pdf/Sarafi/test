{{-- resources/views/pdf/Sarafi/transaction.blade.php --}}
<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="UTF-8">
    <title>سند تراکنش - صرافی اقصی #{{ $transaction->id }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Vazir, "DejaVu Sans", sans-serif;
        }

        body {
            background: #f4f6fa;
            padding: 20px;
            color: #2c2c2c;
            direction: rtl;
        }

        .receipt-container {
            max-width: 720px;
            margin: 0 auto;
            background: #fff;
            border-radius: 12px;
            overflow: hidden;
            border: 1px solid #e0e4eb;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
        }

        .receipt-header {
            background: linear-gradient(90deg, #3B82F6 0%, #1A4BB8 100%);
            color: #fff;
            padding: 20px;
            text-align: center;
            position: relative;
        }

        .receipt-header h1 {
            font-size: 22px;
            font-weight: 700;
            margin-bottom: 8px;
        }

        .receipt-meta {
            font-size: 13px;
            opacity: 0.9;
        }

        .stamp {
            position: absolute;
            top: 14px;
            left: 14px;
            width: 70px;
            height: 70px;
            border-radius: 50%;
            border: 3px solid rgba(255, 255, 255, 0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            color: rgba(255, 255, 255, 0.85);
            transform: rotate(-12deg);
            font-size: 11px;
        }

        .receipt-body {
            padding: 24px 28px;
        }

        .transaction-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 22px;
            font-size: 14px;
        }

        .transaction-table td {
            padding: 12px 14px;
            vertical-align: top;
            border-bottom: 1px solid #f0f0f0;
        }

        .transaction-table tr:last-child td {
            border-bottom: none;
        }

        .field-name {
            width: 34%;
            font-weight: 700;
            background: #f7f9fc;
            color: #1b2a46;
            border-right: 3px solid #e0e4eb;
            padding-right: 16px;
        }

        .divider {
            height: 2px;
            background: linear-gradient(to left, transparent, #3B82F6, transparent);
            margin: 20px 0;
        }

        .description-section {
            background: #f7f9fc;
            padding: 16px 18px;
            border-radius: 8px;
            border-right: 4px solid #3B82F6;
            margin-bottom: 18px;
        }

        .description-title {
            color: #3B82F6;
            font-weight: 700;
            margin-bottom: 10px;
        }

        .contact-info {
            display: flex;
            justify-content: space-between;
            gap: 16px;
            flex-wrap: wrap;
            margin-top: 12px;
        }

        .contact-item {
            min-width: 160px;
            flex: 1;
        }

        .contact-label {
            display: block;
            font-weight: 700;
            color: #3B82F6;
            margin-bottom: 6px;
        }

        .signature-section {
            text-align: right;
            margin-top: 28px;
            padding-top: 16px;
            border-top: 1px dashed #ccc;
        }

        .signature-label {
            display: block;
            font-weight: 700;
            margin-bottom: 10px;
        }

        .note-section {
            background: #fff8e6;
            padding: 14px;
            border-radius: 8px;
            margin-top: 20px;
            font-size: 13px;
            color: #8a6d3b;
            border: 1px solid #ffeaa7;
            text-align: center;
        }

        .receipt-footer {
            background: #f7f9fc;
            padding: 14px;
            text-align: center;
            font-size: 13px;
            color: #6b7280;
            border-top: 1px solid #e0e4eb;
        }

        @media print {
            body {
                background: white;
                padding: 0;
            }

            .receipt-container {
                box-shadow: none;
                border: 1px solid #ccc;
            }
        }
    </style>
</head>

<body>
    <div class="receipt-container">

        <div class="receipt-body">
            @php
                $currenciesFa = [
                    'afn' => 'افغانی',
                    'usd' => 'دالر',
                    'eur' => 'یورو',
                    'irr' => 'تومان',
                    'aed' => 'درهم',
                    'try' => 'لیره',
                    'cny' => 'یوان',
                    'pkr' => 'کلدار',
                    'gbp' => 'پوند',
                    'jpy' => 'ین',
                    'sar' => 'ریال سعودی',
                    'inr' => 'روپیه',
                ];
            @endphp

            <table class="transaction-table">
                <tr>
                    <td class="field-name">حساب</td>
                    <td>
                        {{ $transaction->customer->account_number }} -
                        {{ $transaction->customer->fullname ?? ($transaction->customer_name ?? '-') }}
                    </td>
                </tr>

                <tr>
                    <td class="field-name">مبلغ</td>
                    <td>
                        {{ number_format((float)$transaction->amount) }} /
                        {{ $currenciesFa[strtolower($transaction->currency)] ?? '-' }}
                    </td>
                </tr>

                <tr>
                    <td class="field-name">نوع تراکنش</td>
                    <td>{{ $transaction->type ?? '-' }}</td>
                </tr>

             
                <tr>
                    <td class="field-name">توسط</td>
                    <td>{{ $transaction->by ?? ($transaction->user->name ?? '-') }}</td>
                </tr>

                <tr>
                    <td class="field-name">زون</td>
                    <td>{{ $transaction->zone ?? '-' }}</td>
                </tr>

                <tr>
                    <td class="field-name">شماره سند</td>
                    <td>{{ $transaction->id }}</td>
                </tr>

                 <tr>
                    <td class="field-name">تاریخ</td>
                    <td>{{ $transaction->date }}</td>
                </tr>

                <tr>
                    <td class="field-name">زمان اجرا</td>
                    <td>
                        {{ $transaction->created_at->format('h:i:s') }}
                        {{ $transaction->created_at->format('A') == 'AM' ? 'قبل از ظهر' : 'بعد از ظهر' }}
                    </td>
                </tr>

                @if(!empty($transaction->transaction_file))
                    <tr>
                        <td class="field-name">فایل پیوست</td>
                        <td>
                            @php
                                $filePath = $transaction->transaction_file;
                                $fileUrl = (strpos($filePath, 'http') === 0)
                                    ? $filePath
                                    : asset('storage/' . ltrim($filePath,'/'));
                            @endphp
                            <a href="{{ $fileUrl }}" target="_blank" style="color:#1a4bb8; text-decoration:underline;">
                                {{ basename($filePath) }}
                            </a>
                        </td>
                    </tr>
                @endif
            </table>

            <div class="divider"></div>

            <div class="description-section">
                <div class="description-title">توضیحات</div>
                <div style="line-height:1.6; color:#2b2b2b;">
                    {{ $transaction->description ?? 'بدون توضیحات بیشتر' }}
                </div>
            </div>

            <div class="contact-info">
                <div class="contact-item">
                    <span class="contact-label">موبایل/تماس</span>
                    {{ $transaction->customer->phone ?? ($transaction->phone ?? '-') }}
                </div>
                <div class="contact-item">
                    <span class="contact-label">آدرس</span>
                    {{ $transaction->customer->address ?? ($transaction->address ?? '-') }}
                </div>
                <div class="contact-item">
                    <span class="contact-label">ثبت‌کننده</span>
                    {{ $transaction->user->name ?? ($transaction->user_id ? 'کاربر #' . $transaction->user_id : '-') }}
                </div>
            </div>

            <div class="signature-section">
                <span class="signature-label">امضاء مسئول</span>
                <div style="margin-top:38px; width:220px; border-bottom:1px solid #222;"></div>
            </div>

            <div class="note-section">
                نوت : این سند جهت معلومات چاب شده است ،  و هیچگاه سند پولی محسوب نخواهد شد.
            </div>
        </div>

       

    </div>
</body>
</html>
